import React from 'react';
// import { useHistory, useParams } from 'react-router-dom';

const Show = () => {
  // const history = useHistory();
  // const { id } = useParams();

  return <div></div>;
};

export default Show;
